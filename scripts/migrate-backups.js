require('dotenv').config({quiet:true});const {platformConnection,waitForPlatform,getTenantConnection}=require('../src/config/db');const load=require('../src/models/tenant/loadModels');const {migrateBackups}=require('./lib/migrateBackups');(async()=>{await waitForPlatform();const Tenant=platformConnection.models.Tenant||require('../src/models/platform/Tenant')(platformConnection);for(const t of await Tenant.find({isDeleted:{$ne:true},dbName:{$nin:[null,'']}}).lean()){const c=await getTenantConnection(t.dbName);console.log(t.code||t._id,await migrateBackups(load(c)));}await platformConnection.close();})().catch(e=>{console.error(e);process.exit(1);});
