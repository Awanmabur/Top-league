async function migrateBackups(models){const M=models?.BackupJob;if(!M?.collection)return{scanned:0,normalized:0,quarantined:0};const rows=await M.collection.find({}).toArray();let normalized=0,quarantined=0;for(const r of rows){const set={revision:Math.max(1,Number(r.revision||1)),storage:'Cloudinary',accessType:r.accessType||(r.filePublicId?'authenticated':r.filePath?'legacy':'missing'),artifactVersion:Number(r.artifactVersion||1)};if(r.status==='Running'){set.status='Failed';set.failedAt=new Date();set.failureReason='Interrupted legacy backup run requires a new execution.';}if(r.status==='Completed'&&(!r.checksum||(!r.filePublicId&&!r.filePath))){set.status='Failed';set.failedAt=new Date();set.failureReason='Legacy completed backup cannot be integrity verified.';set.migrationQuarantinedAt=new Date();set.migrationQuarantineReason='Missing checksum or backup artifact.';quarantined++;}if(r.scope==='Media Only'||r.scope==='Database Only')set.scope='Full System';await M.collection.updateOne({_id:r._id},{$set:set});normalized++;}return{scanned:rows.length,normalized,quarantined};}
module.exports={migrateBackups};
