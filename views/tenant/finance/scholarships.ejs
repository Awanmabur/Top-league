<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Admin • Scholarships — Classic Academy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/css/navbar.css" />
  <link rel="stylesheet" href="/css/student-portal.css" />

  <style>
    .toolbar{display:flex;gap:10px;flex-wrap:wrap;justify-content:space-between;align-items:center}
    .toolbar .left,.toolbar .right{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
    .input,.select,.textarea{padding:10px 12px;border-radius:12px;border:1px solid rgba(13,64,96,.16);background:#fff;outline:none}
    .input{min-width:220px}
    .textarea{width:100%;min-height:110px;resize:vertical}
    .input:focus,.select:focus,.textarea:focus{border-color:rgba(10,111,191,.45);box-shadow:0 0 0 3px rgba(10,111,191,.12)}
    .muted{font-size:12px;color:#6b7280}
    .strong{font-weight:900}
    .grid-4{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}
    .grid-2{display:grid;grid-template-columns:minmax(0,1.24fr) minmax(320px,.76fr);gap:12px}
    .chips{display:flex;gap:8px;flex-wrap:wrap}
    .chip{border:1px solid rgba(13,64,96,.14);background:#fff;border-radius:999px;padding:8px 12px;cursor:pointer;font-weight:900;font-size:12px}
    .chip.active{background:rgba(10,111,191,.12);border-color:rgba(10,111,191,.35)}
    .pill{display:inline-flex;align-items:center;gap:6px;padding:4px 10px;border-radius:999px;font-size:12px;font-weight:900;white-space:nowrap}
    .ok{background:#ecfdf3;color:#15803d}
    .warn{background:#fef9c3;color:#92400e}
    .bad{background:#fee2e2;color:#b91c1c}
    .info{background:#eef2ff;color:#3730a3}
    .draft{background:rgba(13,64,96,.08);color:#0f172a}
    .table-wrap{overflow:auto;border:1px solid rgba(13,64,96,.10);border-radius:12px;max-width:100%}
    table{width:100%;border-collapse:separate;border-spacing:0;min-width:1180px;background:#fff}
    th,td{padding:10px 12px;border-bottom:1px solid rgba(13,64,96,.08);vertical-align:middle}
    th{font-size:12px;color:#6b7280;text-transform:uppercase;letter-spacing:.04em;text-align:left;background:rgba(13,64,96,.03);position:sticky;top:0;z-index:1}
    tr:hover td{background:rgba(10,111,191,.04)}
    .actions{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap;max-width:100%}
    .btn-xs{padding:6px 8px;border-radius:10px;border:1px solid rgba(13,64,96,.14);background:#fff;cursor:pointer;font-size:12px;flex:0 0 auto}
    .btn-xs:hover{background:rgba(10,111,191,.06)}
    .bulkbar{display:none;margin-top:10px;padding:10px 12px;border-radius:12px;background:rgba(10,111,191,.08);border:1px solid rgba(10,111,191,.18);justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap}
    .bulkbar.show{display:flex}
    .badge-count{display:inline-flex;align-items:center;justify-content:center;min-width:26px;height:22px;padding:0 8px;border-radius:999px;background:#0a6fbf;color:#fff;font-size:12px;font-weight:900}
    .note{padding:10px 12px;border-radius:12px;border:1px dashed rgba(13,64,96,.18);background:rgba(13,64,96,.03)}
    .mini-list{display:grid;gap:10px;margin-top:10px}
    .mini-item{display:flex;justify-content:space-between;gap:10px;align-items:center;padding:10px 12px;border:1px solid rgba(13,64,96,.10);border-radius:12px;background:#fff}
    .mini-item .left{display:grid;gap:2px}
    .flash-wrap{display:grid;gap:10px;margin-bottom:12px}
    .flash{padding:12px 14px;border-radius:12px;font-weight:700}
    .flash-success{background:#ecfdf3;color:#166534;border:1px solid #bbf7d0}
    .flash-error{background:#fef2f2;color:#991b1b;border:1px solid #fecaca}
    .topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;flex-wrap:nowrap}
    .topbar > div:first-child{min-width:0;flex:1 1 auto}
    .top-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end;align-items:center;flex:0 0 auto;margin-left:auto}
    .modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.45);display:none;align-items:center;justify-content:center;padding:30px 18px;z-index:9999;overflow-y:auto;overflow-x:hidden}
    .modal-backdrop.show{display:flex}
    .modal{width:min(1040px,100%);max-height:calc(100dvh - 60px);background:#fff;border-radius:22px;box-shadow:0 24px 70px rgba(0,0,0,.25);overflow:hidden;display:flex;flex-direction:column;margin:auto}
    .modal.sm{width:min(840px,100%)}
    .modal.lg{width:min(1080px,100%)}
    .modal > form{display:flex;flex-direction:column;flex:1 1 auto;min-height:0;overflow:hidden}
    .modal-header{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:16px 18px;border-bottom:1px solid rgba(13,64,96,.10);flex:0 0 auto;background:#fff}
    .modal-title{font-weight:900;font-size:18px}
    .modal-body{padding:16px 18px;flex:1 1 auto;min-height:0;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch}
    .modal-footer{display:flex;justify-content:flex-end;gap:10px;padding:14px 18px;border-top:1px solid rgba(13,64,96,.10);flex:0 0 auto;background:#fff;flex-wrap:wrap;position:relative;z-index:2;box-shadow:0 -8px 18px rgba(0,0,0,.04)}
    .form-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}
    .field label{display:block;font-size:12px;color:#6b7280;margin-bottom:6px}
    .field input,.field select{width:100%}
    .divider{height:1px;background:rgba(13,64,96,.10);margin:12px 0}
    .json-data{display:none !important}
    .view-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:14px}
    .view-card{padding:14px;border:1px solid rgba(13,64,96,.10);border-radius:16px;background:#fff;min-width:0}
    .view-label{font-size:12px;color:#6b7280;margin-bottom:6px}
    .view-value{font-weight:800;word-break:break-word}
    .view-message{padding:14px;border:1px solid rgba(13,64,96,.10);border-radius:16px;background:#fff;white-space:pre-wrap;line-height:1.65}
    .amount-lg{font-size:24px;font-weight:900}
    @media (max-width: 1100px){.grid-2{grid-template-columns:1fr}}
    @media (max-width: 980px){.form-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.topbar{flex-wrap:wrap}.top-actions{width:100%;justify-content:flex-start}}
    @media (max-width: 680px){.grid-4{grid-template-columns:repeat(2,minmax(0,1fr))}.input{min-width:160px}.view-grid{grid-template-columns:1fr}.form-grid{grid-template-columns:1fr}}
    @media (max-width: 520px){
      .grid-4{grid-template-columns:repeat(2,minmax(0,1fr))}
      .toolbar .left,.toolbar .right{width:100%}
      .toolbar .left > *,.toolbar .right > *{max-width:100%}
      .actions{justify-content:flex-start}
      table{min-width:1080px}
      .modal-backdrop{padding:30px 12px}
      .modal{max-height:calc(100dvh - 60px);border-radius:20px}
      .modal-header,.modal-body,.modal-footer{padding-left:14px;padding-right:14px}
    }
  </style>
</head>
<body data-portal="tenant">
  <%
    const q = query || {};
    const formatMoney = (v) => Number(v || 0).toLocaleString(undefined,{minimumFractionDigits:0,maximumFractionDigits:2});
  %>

  <div class="app">
    <%- include("../partials/navbar") %>

    <main class="main">
      <% if (typeof messages !== "undefined" && (messages.success?.length || messages.error?.length)) { %>
        <div class="flash-wrap">
          <% (messages.success || []).forEach(function(msg){ %>
            <div class="flash flash-success"><%= msg %></div>
          <% }) %>
          <% (messages.error || []).forEach(function(msg){ %>
            <div class="flash flash-error"><%= msg %></div>
          <% }) %>
        </div>
      <% } %>

      <div class="topbar">
        <div>
          <div class="title">Scholarships</div>
          <div class="subtitle">Manage scholarship awards, waivers, grants and fee relief by student or subject.</div>
        </div>

        <div class="top-actions">
          <a class="btn light" href="/admin/finance"><i class="fa-solid fa-arrow-left"></i> Finance</a>
          <a class="btn light" href="/admin/fee-structures"><i class="fa-solid fa-layer-group"></i> Fee Structure</a>
          <button class="btn light" id="btnExport" type="button"><i class="fa-solid fa-file-arrow-down"></i> Export</button>
          <button class="btn primary" id="btnCreate" type="button"><i class="fa-solid fa-plus"></i> Create Scholarship</button>
        </div>
      </div>

      <div class="grid-4">
        <div class="card">
          <div class="section-title">Scholarships</div>
          <div class="kpi-value"><%= kpis?.total || 0 %></div>
          <div class="muted">Total scholarship records</div>
        </div>
        <div class="card">
          <div class="section-title">Active</div>
          <div class="kpi-value" style="color:#15803d"><%= kpis?.active || 0 %></div>
          <div class="muted">Currently effective</div>
        </div>
        <div class="card">
          <div class="section-title">Fixed Amount Total</div>
          <div class="kpi-value" style="color:#0a6fbf"><%= formatMoney(kpis?.amountTotal || 0) %></div>
          <div class="muted">Total fixed-value awards</div>
        </div>
        <div class="card">
          <div class="section-title">Full Awards</div>
          <div class="kpi-value" style="color:#92400e"><%= kpis?.fullCount || 0 %></div>
          <div class="muted">100% coverage awards</div>
        </div>
      </div>

      <section class="card" style="margin-top:12px;">
        <form class="toolbar" method="GET" action="/admin/scholarships">
          <div class="left">
            <input class="input" name="q" value="<%= q.q || '' %>" placeholder="Search name, code, sponsor, notes..." />

            <select class="select" name="status">
              <option value="all" <%= q.status === 'all' ? 'selected' : '' %>>All Status</option>
              <option value="Active" <%= q.status === 'Active' ? 'selected' : '' %>>Active</option>
              <option value="Inactive" <%= q.status === 'Inactive' ? 'selected' : '' %>>Inactive</option>
              <option value="Expired" <%= q.status === 'Expired' ? 'selected' : '' %>>Expired</option>
              <option value="Revoked" <%= q.status === 'Revoked' ? 'selected' : '' %>>Revoked</option>
            </select>

            <select class="select" name="type">
              <option value="all" <%= q.type === 'all' ? 'selected' : '' %>>All Types</option>
              <option value="Percentage" <%= q.type === 'Percentage' ? 'selected' : '' %>>Percentage</option>
              <option value="Fixed Amount" <%= q.type === 'Fixed Amount' ? 'selected' : '' %>>Fixed Amount</option>
              <option value="Full" <%= q.type === 'Full' ? 'selected' : '' %>>Full</option>
            </select>

            <select class="select" name="student">
              <option value="all" <%= q.student === 'all' ? 'selected' : '' %>>All Students</option>
              <% (students || []).forEach(function(s){ %>
                <option value="<%= s.id %>" <%= q.student === s.id ? 'selected' : '' %>><%= s.name %></option>
              <% }) %>
            </select>

            <select class="select" name="program">
              <option value="all" <%= q.program === 'all' ? 'selected' : '' %>>All Subjects</option>
              <% (programs || []).forEach(function(p){ %>
                <option value="<%= p.id %>" <%= q.program === p.id ? 'selected' : '' %>><%= p.name %></option>
              <% }) %>
            </select>
          </div>

          <div class="right">
            <div class="chips" id="viewChips">
              <button class="chip active" data-view="list" type="button"><i class="fa-solid fa-list"></i> List</button>
              <button class="chip" data-view="awards" type="button"><i class="fa-solid fa-award"></i> Awards</button>
              <button class="chip" data-view="summary" type="button"><i class="fa-solid fa-chart-pie"></i> Summary</button>
            </div>
            <button class="btn light" id="btnBulk" type="button"><i class="fa-solid fa-layer-group"></i> Bulk</button>
            <a class="btn light" href="/admin/scholarships"><i class="fa-solid fa-rotate-left"></i> Reset</a>
            <button class="btn primary" type="submit"><i class="fa-solid fa-magnifying-glass"></i> Filter</button>
          </div>
        </form>

        <div class="bulkbar" id="bulkbar">
          <div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap">
            <strong>Selected</strong> <span class="badge-count" id="selCount">0</span>
            <span class="muted">scholarship(s)</span>
          </div>
          <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center">
            <button class="btn-xs" id="bulkActivate" type="button"><i class="fa-solid fa-circle-check"></i> Activate</button>
            <button class="btn-xs" id="bulkInactive" type="button"><i class="fa-solid fa-clock"></i> Inactive</button>
            <button class="btn-xs" id="bulkExpire" type="button"><i class="fa-solid fa-calendar-xmark"></i> Expire</button>
            <button class="btn-xs" id="bulkRevoke" type="button"><i class="fa-solid fa-ban"></i> Revoke</button>
            <button class="btn-xs" id="bulkDelete" type="button"><i class="fa-solid fa-trash"></i> Delete</button>
            <button class="btn-xs" id="bulkClear" type="button"><i class="fa-solid fa-xmark"></i> Clear</button>
          </div>
        </div>
      </section>

      <div class="grid-2" style="margin-top:12px;">
        <section class="card" style="min-width:0;">
          <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;align-items:center;">
            <div>
              <div class="section-title" id="panelTitle">Scholarships</div>
              <div class="section-sub" id="panelSub">Manage scholarship records, scope, value and lifecycle status.</div>
            </div>
            <div class="muted" id="resultMeta">—</div>
          </div>

          <div id="view-list" style="margin-top:10px;">
            <div class="table-wrap">
              <table>
                <thead>
                  <tr>
                    <th style="width:42px;"><input type="checkbox" id="checkAll" /></th>
                    <th>Scholarship</th>
                    <th>Student</th>
                    <th>Subject</th>
                    <th>Type</th>
                    <th>Value</th>
                    <th>Period</th>
                    <th>Status</th>
                    <th style="text-align:right;width:260px;">Actions</th>
                  </tr>
                </thead>
                <tbody id="tbody"></tbody>
              </table>
            </div>
          </div>

          <div id="view-awards" style="display:none;margin-top:10px;">
            <div class="note">Award view summarizes scholarship allocation targets and values.</div>
            <div class="table-wrap" style="margin-top:10px;">
              <table>
                <thead>
                  <tr>
                    <th>Name</th>
                    <th>Code</th>
                    <th>Sponsor</th>
                    <th>Student</th>
                    <th>Subject</th>
                    <th>Type</th>
                    <th>Value</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody id="tbodyAwards"></tbody>
              </table>
            </div>
          </div>

          <div id="view-summary" style="display:none;margin-top:10px;">
            <div class="grid-4">
              <div class="view-card">
                <div class="view-label">Percentage</div>
                <div class="amount-lg" style="color:#0a6fbf"><%= kpis?.percentageCount || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Fixed Amount</div>
                <div class="amount-lg" style="color:#15803d"><%= kpis?.fixedCount || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Full</div>
                <div class="amount-lg" style="color:#92400e"><%= kpis?.fullCount || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Revoked</div>
                <div class="amount-lg" style="color:#b91c1c"><%= kpis?.revoked || 0 %></div>
              </div>
            </div>

            <div class="note" style="margin-top:12px;">
              <strong>Snapshot:</strong>
              <span>Total records: <%= kpis?.total || 0 %></span> ·
              <span>Active: <%= kpis?.active || 0 %></span> ·
              <span>Fixed amount total: <%= formatMoney(kpis?.amountTotal || 0) %></span>
            </div>
          </div>
        </section>

        <aside class="card" style="min-width:0;">
          <div class="section-title">Quick Actions</div>
          <div class="section-sub">Common scholarship workflows.</div>

          <div class="mini-list">
            <div class="mini-item">
              <div class="left">
                <div class="strong">New Scholarship</div>
                <div class="muted">Create a new award</div>
              </div>
              <button class="btn-xs" id="quickNewScholarship" type="button"><i class="fa-solid fa-plus"></i></button>
            </div>
            <div class="mini-item">
              <div class="left">
                <div class="strong">Active Only</div>
                <div class="muted">Review current awards</div>
              </div>
              <a class="btn-xs" href="/admin/scholarships?status=Active"><i class="fa-solid fa-filter"></i></a>
            </div>
            <div class="mini-item">
              <div class="left">
                <div class="strong">Go to Fee Structures</div>
                <div class="muted">Review billing templates</div>
              </div>
              <a class="btn-xs" href="/admin/fee-structures"><i class="fa-solid fa-layer-group"></i></a>
            </div>
            <div class="mini-item">
              <div class="left">
                <div class="strong">Go to Statements</div>
                <div class="muted">Review balances and activity</div>
              </div>
              <a class="btn-xs" href="/admin/student-statements"><i class="fa-solid fa-book"></i></a>
            </div>
          </div>
        </aside>
      </div>
    </main>
  </div>

  <div class="modal-backdrop" id="mEdit">
    <div class="modal lg">
      <div class="modal-header">
        <div class="modal-title" id="mTitle">Create Scholarship</div>
        <button class="btn-xs" type="button" data-close-modal="mEdit"><i class="fa-solid fa-xmark"></i></button>
      </div>

      <form id="scholarshipForm" method="POST" action="/admin/scholarships">
        <% if (csrfToken) { %><input type="hidden" name="_csrf" value="<%= csrfToken %>"><% } %>

        <div class="modal-body">
          <div class="form-grid">
            <div class="field">
              <label>Name</label>
              <input class="input" id="sName" name="name" placeholder="e.g. Merit Scholarship 2026" required>
            </div>

            <div class="field">
              <label>Code</label>
              <input class="input" id="sCode" name="code" placeholder="Optional code">
            </div>

            <div class="field">
              <label>Student</label>
              <select class="select" id="sStudent" name="studentId">
                <option value="">Select student</option>
                <% (students || []).forEach(function(s){ %>
                  <option value="<%= s.id %>"><%= s.name %></option>
                <% }) %>
              </select>
            </div>

            <div class="field">
              <label>Subject</label>
              <select class="select" id="sProgram" name="programId">
                <option value="">Select program</option>
                <% (programs || []).forEach(function(p){ %>
                  <option value="<%= p.id %>"><%= p.name %></option>
                <% }) %>
              </select>
            </div>

            <div class="field">
              <label>Type</label>
              <select class="select" id="sType" name="type">
                <option>Fixed Amount</option>
                <option>Percentage</option>
                <option>Full</option>
              </select>
            </div>

            <div class="field" id="valueField">
              <label>Percentage Value</label>
              <input class="input" id="sValue" name="value" type="number" min="0" step="0.01" value="0">
            </div>

            <div class="field" id="amountField">
              <label>Amount</label>
              <input class="input" id="sAmount" name="amount" type="number" min="0" step="0.01" value="0">
            </div>

            <div class="field">
              <label>Sponsor</label>
              <input class="input" id="sSponsor" name="sponsor" placeholder="Sponsor / donor / source">
            </div>

            <div class="field">
              <label>Start Date</label>
              <input class="input" id="sStartDate" name="startDate" type="date">
            </div>

            <div class="field">
              <label>End Date</label>
              <input class="input" id="sEndDate" name="endDate" type="date">
            </div>

            <div class="field">
              <label>Status</label>
              <select class="select" id="sStatus" name="status">
                <option>Active</option>
                <option>Inactive</option>
                <option>Expired</option>
                <option>Revoked</option>
              </select>
            </div>
          </div>

          <div class="divider"></div>

          <div class="view-grid">
            <div class="view-card">
              <div class="view-label">Scholarship Type</div>
              <div class="view-value" id="typePreview">Fixed Amount</div>
            </div>
            <div class="view-card">
              <div class="view-label">Current Value Preview</div>
              <div class="view-value" id="valuePreview">0</div>
            </div>
          </div>

          <div class="field">
            <label>Notes</label>
            <textarea class="textarea" id="sNotes" name="notes" placeholder="Optional notes about this scholarship..."></textarea>
          </div>
        </div>

        <div class="modal-footer">
          <button class="btn light" type="button" data-close-modal="mEdit">Cancel</button>
          <button class="btn primary" type="submit"><i class="fa-solid fa-floppy-disk"></i> Save Scholarship</button>
        </div>
      </form>
    </div>
  </div>

  <div class="modal-backdrop" id="mView">
    <div class="modal sm">
      <div class="modal-header">
        <div class="modal-title">Scholarship Details</div>
        <button class="btn-xs" type="button" data-close-modal="mView"><i class="fa-solid fa-xmark"></i></button>
      </div>

      <div class="modal-body">
        <div class="view-grid">
          <div class="view-card">
            <div class="view-label">Name</div>
            <div class="view-value" id="vName">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Code</div>
            <div class="view-value" id="vCode">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Student</div>
            <div class="view-value" id="vStudent">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Subject</div>
            <div class="view-value" id="vProgram">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Type</div>
            <div class="view-value" id="vType">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Value</div>
            <div class="view-value" id="vValue">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Sponsor</div>
            <div class="view-value" id="vSponsor">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Status</div>
            <div class="view-value" id="vStatus">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Start Date</div>
            <div class="view-value" id="vStartDate">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">End Date</div>
            <div class="view-value" id="vEndDate">—</div>
          </div>
        </div>

        <div class="section-title">Notes</div>
        <div class="view-message" id="vNotes">—</div>
      </div>

      <div class="modal-footer">
        <button class="btn light" type="button" data-close-modal="mView">Close</button>
      </div>
    </div>
  </div>

  <form id="rowActionForm" method="POST" style="display:none;">
    <% if (csrfToken) { %><input type="hidden" name="_csrf" value="<%= csrfToken %>"><% } %>
  </form>

  <form id="bulkForm" method="POST" action="/admin/scholarships/bulk" style="display:none;">
    <% if (csrfToken) { %><input type="hidden" name="_csrf" value="<%= csrfToken %>"><% } %>
    <input type="hidden" name="ids" id="bulkIds">
    <input type="hidden" name="action" id="bulkActionInput">
  </form>

  <textarea id="scholarshipsData" class="json-data" aria-hidden="true"><%- JSON.stringify(scholarships || []) %></textarea>

  <script src="/js/navbar.js"></script>
  <script src="/js/scholarships.js"></script>
</body>
</html>
