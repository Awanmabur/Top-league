<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>Admin • Student Statements — Classic Academy</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        crossorigin="anonymous" referrerpolicy="no-referrer" />
  <link rel="stylesheet" href="/css/navbar.css" />
  <link rel="stylesheet" href="/css/student-portal.css" />

  <style>
    .toolbar{display:flex;gap:10px;flex-wrap:wrap;justify-content:space-between;align-items:center}
    .toolbar .left,.toolbar .right{display:flex;gap:8px;flex-wrap:wrap;align-items:center}
    .input,.select,.textarea{padding:10px 12px;border-radius:12px;border:1px solid rgba(13,64,96,.16);background:#fff;outline:none}
    .input{min-width:220px}
    .textarea{width:100%;min-height:110px;resize:vertical}
    .input:focus,.select:focus,.textarea:focus{border-color:rgba(10,111,191,.45);box-shadow:0 0 0 3px rgba(10,111,191,.12)}
    .muted{font-size:12px;color:#6b7280}
    .strong{font-weight:900}
    .grid-4{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}
    .grid-2{display:grid;grid-template-columns:minmax(0,1.18fr) minmax(340px,.82fr);gap:12px}
    .chips{display:flex;gap:8px;flex-wrap:wrap}
    .chip{border:1px solid rgba(13,64,96,.14);background:#fff;border-radius:999px;padding:8px 12px;cursor:pointer;font-weight:900;font-size:12px}
    .chip.active{background:rgba(10,111,191,.12);border-color:rgba(10,111,191,.35)}
    .pill{display:inline-flex;align-items:center;gap:6px;padding:4px 10px;border-radius:999px;font-size:12px;font-weight:900;white-space:nowrap}
    .ok{background:#ecfdf3;color:#15803d}
    .warn{background:#fef9c3;color:#92400e}
    .bad{background:#fee2e2;color:#b91c1c}
    .info{background:#eef2ff;color:#3730a3}
    .table-wrap{overflow:auto;border:1px solid rgba(13,64,96,.10);border-radius:12px;max-width:100%}
    table{width:100%;border-collapse:separate;border-spacing:0;min-width:1120px;background:#fff}
    th,td{padding:10px 12px;border-bottom:1px solid rgba(13,64,96,.08);vertical-align:middle}
    th{font-size:12px;color:#6b7280;text-transform:uppercase;letter-spacing:.04em;text-align:left;background:rgba(13,64,96,.03);position:sticky;top:0;z-index:1}
    tr:hover td{background:rgba(10,111,191,.04)}
    .actions{display:flex;gap:6px;justify-content:flex-end;flex-wrap:wrap;max-width:100%}
    .btn-xs{padding:6px 8px;border-radius:10px;border:1px solid rgba(13,64,96,.14);background:#fff;cursor:pointer;font-size:12px;flex:0 0 auto}
    .btn-xs:hover{background:rgba(10,111,191,.06)}
    .topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;flex-wrap:nowrap}
    .topbar > div:first-child{min-width:0;flex:1 1 auto}
    .top-actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end;align-items:center;flex:0 0 auto;margin-left:auto}
    .note{padding:10px 12px;border-radius:12px;border:1px dashed rgba(13,64,96,.18);background:rgba(13,64,96,.03)}
    .mini-list{display:grid;gap:10px;margin-top:10px}
    .mini-item{display:flex;justify-content:space-between;gap:10px;align-items:center;padding:10px 12px;border:1px solid rgba(13,64,96,.10);border-radius:12px;background:#fff}
    .mini-item .left{display:grid;gap:2px}
    .flash-wrap{display:grid;gap:10px;margin-bottom:12px}
    .flash{padding:12px 14px;border-radius:12px;font-weight:700}
    .flash-success{background:#ecfdf3;color:#166534;border:1px solid #bbf7d0}
    .flash-error{background:#fef2f2;color:#991b1b;border:1px solid #fecaca}
    .json-data{display:none !important}
    .modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.45);display:none;align-items:center;justify-content:center;padding:30px 18px;z-index:9999;overflow-y:auto;overflow-x:hidden}
    .modal-backdrop.show{display:flex}
    .modal{width:min(1120px,100%);max-height:calc(100dvh - 60px);background:#fff;border-radius:22px;box-shadow:0 24px 70px rgba(0,0,0,.25);overflow:hidden;display:flex;flex-direction:column;margin:auto}
    .modal.sm{width:min(860px,100%)}
    .modal-header{display:flex;justify-content:space-between;align-items:center;gap:12px;padding:16px 18px;border-bottom:1px solid rgba(13,64,96,.10);flex:0 0 auto;background:#fff}
    .modal-title{font-weight:900;font-size:18px}
    .modal-body{padding:16px 18px;flex:1 1 auto;min-height:0;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch}
    .modal-footer{display:flex;justify-content:flex-end;gap:10px;padding:14px 18px;border-top:1px solid rgba(13,64,96,.10);flex:0 0 auto;background:#fff;flex-wrap:wrap}
    .view-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:14px}
    .view-card{padding:14px;border:1px solid rgba(13,64,96,.10);border-radius:16px;background:#fff;min-width:0}
    .view-label{font-size:12px;color:#6b7280;margin-bottom:6px}
    .view-value{font-weight:800;word-break:break-word}
    .amount-lg{font-size:24px;font-weight:900}
    @media (max-width: 1100px){.grid-2{grid-template-columns:1fr}}
    @media (max-width: 980px){.topbar{flex-wrap:wrap}.top-actions{width:100%;justify-content:flex-start}}
    @media (max-width: 680px){.grid-4{grid-template-columns:repeat(2,minmax(0,1fr))}.input{min-width:160px}.view-grid{grid-template-columns:1fr}}
    @media (max-width: 520px){
      .grid-4{grid-template-columns:repeat(2,minmax(0,1fr))}
      .toolbar .left,.toolbar .right{width:100%}
      .toolbar .left > *,.toolbar .right > *{max-width:100%}
      .actions{justify-content:flex-start}
      table{min-width:1000px}
      .modal-backdrop{padding:30px 12px}
      .modal{max-height:calc(100dvh - 60px);border-radius:20px}
      .modal-header,.modal-body,.modal-footer{padding-left:14px;padding-right:14px}
    }
  </style>
</head>
<body>
  <%
    const q = query || {};
    const formatMoney = (v) => Number(v || 0).toLocaleString(undefined,{minimumFractionDigits:0,maximumFractionDigits:2});
  %>

  <div class="app">
    <%- include("../partials/navbar") %>

    <main class="main">
      <% if (typeof messages !== "undefined" && (messages.success?.length || messages.error?.length)) { %>
        <div class="flash-wrap">
          <% (messages.success || []).forEach(function(msg){ %>
            <div class="flash flash-success"><%= msg %></div>
          <% }) %>
          <% (messages.error || []).forEach(function(msg){ %>
            <div class="flash flash-error"><%= msg %></div>
          <% }) %>
        </div>
      <% } %>

      <div class="topbar">
        <div>
          <div class="title">Student Statements</div>
          <div class="subtitle">View student account statements from invoices and payments, including running balances and activity history.</div>
        </div>

        <div class="top-actions">
          <a class="btn light" href="/admin/finance"><i class="fa-solid fa-arrow-left"></i> Finance</a>
          <a class="btn light" href="/admin/invoices"><i class="fa-solid fa-file-invoice-dollar"></i> Invoices</a>
          <a class="btn light" href="/admin/payments"><i class="fa-solid fa-money-bill-wave"></i> Payments</a>
          <button class="btn light" id="btnExport" type="button"><i class="fa-solid fa-file-arrow-down"></i> Export</button>
        </div>
      </div>

      <div class="grid-4">
        <div class="card">
          <div class="section-title">Students</div>
          <div class="kpi-value"><%= totals?.students || 0 %></div>
          <div class="muted">Students in current result</div>
        </div>
        <div class="card">
          <div class="section-title">Invoiced</div>
          <div class="kpi-value" style="color:#0a6fbf"><%= formatMoney(totals?.invoiced || 0) %></div>
          <div class="muted">Total billed value</div>
        </div>
        <div class="card">
          <div class="section-title">Paid</div>
          <div class="kpi-value" style="color:#15803d"><%= formatMoney(totals?.paid || 0) %></div>
          <div class="muted">Total received value</div>
        </div>
        <div class="card">
          <div class="section-title">Balance</div>
          <div class="kpi-value" style="color:#b91c1c"><%= formatMoney(totals?.balance || 0) %></div>
          <div class="muted">Outstanding student balance</div>
        </div>
      </div>

      <section class="card" style="margin-top:12px;">
        <form class="toolbar" method="GET" action="/admin/student-statements">
          <div class="left">
            <input class="input" name="q" value="<%= q.q || '' %>" placeholder="Search student, admission no, or program..." />

            <select class="select" name="student">
              <option value="all" <%= q.student === 'all' ? 'selected' : '' %>>All Students</option>
              <% (students || []).forEach(function(s){ %>
                <option value="<%= s.id %>" <%= q.student === s.id ? 'selected' : '' %>><%= s.name %></option>
              <% }) %>
            </select>

            <select class="select" name="program">
              <option value="all" <%= q.program === 'all' ? 'selected' : '' %>>All Programs</option>
              <% (programs || []).forEach(function(p){ %>
                <option value="<%= p.id %>" <%= q.program === p.id ? 'selected' : '' %>><%= p.name %></option>
              <% }) %>
            </select>
          </div>

          <div class="right">
            <div class="chips" id="viewChips">
              <button class="chip active" data-view="list" type="button"><i class="fa-solid fa-list"></i> List</button>
              <button class="chip" data-view="ledger" type="button"><i class="fa-solid fa-book"></i> Ledger</button>
              <button class="chip" data-view="summary" type="button"><i class="fa-solid fa-chart-pie"></i> Summary</button>
            </div>
            <a class="btn light" href="/admin/student-statements"><i class="fa-solid fa-rotate-left"></i> Reset</a>
            <button class="btn primary" type="submit"><i class="fa-solid fa-magnifying-glass"></i> Filter</button>
          </div>
        </form>
      </section>

      <div class="grid-2" style="margin-top:12px;">
        <section class="card" style="min-width:0;">
          <div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap;align-items:center;">
            <div>
              <div class="section-title" id="panelTitle">Statements</div>
              <div class="section-sub" id="panelSub">Student account summaries with billed, paid and balance values.</div>
            </div>
            <div class="muted" id="resultMeta">—</div>
          </div>

          <div id="view-list" style="margin-top:10px;">
            <div class="table-wrap">
              <table>
                <thead>
                  <tr>
                    <th>Student</th>
                    <th>Program</th>
                    <th>Invoices</th>
                    <th>Payments</th>
                    <th>Total Invoiced</th>
                    <th>Total Paid</th>
                    <th>Balance</th>
                    <th style="text-align:right;width:220px;">Actions</th>
                  </tr>
                </thead>
                <tbody id="tbody"></tbody>
              </table>
            </div>
          </div>

          <div id="view-ledger" style="display:none;margin-top:10px;">
            <div class="note">Select a student row from the list or use “Open” to inspect ledger movements.</div>

            <div class="table-wrap" style="margin-top:10px;">
              <table>
                <thead>
                  <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Reference</th>
                    <th>Description</th>
                    <th>Program</th>
                    <th>Debit</th>
                    <th>Credit</th>
                    <th>Running Balance</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody id="tbodyLedger"></tbody>
              </table>
            </div>
          </div>

          <div id="view-summary" style="display:none;margin-top:10px;">
            <div class="grid-4">
              <div class="view-card">
                <div class="view-label">Students</div>
                <div class="amount-lg"><%= totals?.students || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Invoices</div>
                <div class="amount-lg" style="color:#0a6fbf"><%= totals?.invoices || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Payments</div>
                <div class="amount-lg" style="color:#15803d"><%= totals?.payments || 0 %></div>
              </div>
              <div class="view-card">
                <div class="view-label">Outstanding</div>
                <div class="amount-lg" style="color:#b91c1c"><%= formatMoney(totals?.balance || 0) %></div>
              </div>
            </div>

            <div class="note" style="margin-top:12px;">
              <strong>Snapshot:</strong>
              <span>Billed: <%= formatMoney(totals?.invoiced || 0) %></span> ·
              <span>Paid: <%= formatMoney(totals?.paid || 0) %></span> ·
              <span>Balance: <%= formatMoney(totals?.balance || 0) %></span>
            </div>
          </div>
        </section>

        <aside class="card" style="min-width:0;">
          <div class="section-title">Quick Actions</div>
          <div class="section-sub">Common statement workflows.</div>

          <div class="mini-list">
            <div class="mini-item">
              <div class="left">
                <div class="strong">All Statements</div>
                <div class="muted">View every student account</div>
              </div>
              <a class="btn-xs" href="/admin/student-statements"><i class="fa-solid fa-list"></i></a>
            </div>

            <div class="mini-item">
              <div class="left">
                <div class="strong">Go to Invoices</div>
                <div class="muted">Review billing records</div>
              </div>
              <a class="btn-xs" href="/admin/invoices"><i class="fa-solid fa-file-invoice-dollar"></i></a>
            </div>

            <div class="mini-item">
              <div class="left">
                <div class="strong">Go to Payments</div>
                <div class="muted">Review collection records</div>
              </div>
              <a class="btn-xs" href="/admin/payments"><i class="fa-solid fa-money-bill-wave"></i></a>
            </div>

            <div class="mini-item">
              <div class="left">
                <div class="strong">Finance Reports</div>
                <div class="muted">Open broader finance analytics</div>
              </div>
              <a class="btn-xs" href="/admin/finance-reports"><i class="fa-solid fa-chart-column"></i></a>
            </div>
          </div>
        </aside>
      </div>
    </main>
  </div>

  <div class="modal-backdrop" id="mView">
    <div class="modal sm">
      <div class="modal-header">
        <div class="modal-title">Student Statement</div>
        <button class="btn-xs" type="button" data-close-modal="mView"><i class="fa-solid fa-xmark"></i></button>
      </div>

      <div class="modal-body">
        <div class="view-grid">
          <div class="view-card">
            <div class="view-label">Student</div>
            <div class="view-value" id="vStudentName">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Program</div>
            <div class="view-value" id="vProgramName">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Admission No</div>
            <div class="view-value" id="vAdmissionNo">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Invoices / Payments</div>
            <div class="view-value" id="vCounts">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Total Invoiced</div>
            <div class="view-value" id="vTotalInvoiced">—</div>
          </div>
          <div class="view-card">
            <div class="view-label">Total Paid</div>
            <div class="view-value" id="vTotalPaid">—</div>
          </div>
          <div class="view-card" style="grid-column:1 / -1;">
            <div class="view-label">Balance</div>
            <div class="view-value" id="vBalance">—</div>
          </div>
        </div>

        <div class="section-title">Ledger Preview</div>
        <div class="table-wrap" style="margin-top:10px;">
          <table style="min-width:920px;">
            <thead>
              <tr>
                <th>Date</th>
                <th>Type</th>
                <th>Reference</th>
                <th>Description</th>
                <th>Debit</th>
                <th>Credit</th>
                <th>Running Balance</th>
              </tr>
            </thead>
            <tbody id="tbodyModalLedger"></tbody>
          </table>
        </div>
      </div>

      <div class="modal-footer">
        <button class="btn light" id="btnPrintStatement" type="button"><i class="fa-solid fa-print"></i> Print</button>
        <button class="btn light" id="btnExportStatement" type="button"><i class="fa-solid fa-file-arrow-down"></i> Export</button>
        <button class="btn primary" type="button" data-close-modal="mView">Close</button>
      </div>
    </div>
  </div>

  <textarea id="statementsData" class="json-data" aria-hidden="true"><%- JSON.stringify(statements || []) %></textarea>

  <script src="/js/navbar.js"></script>
  <script src="/js/student-statements.js"></script>
</body>
</html>