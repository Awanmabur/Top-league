const express=require('express');const router=express.Router();const ctrl=require('../../../controllers/tenant/admin/system-healthController');
router.get('/',ctrl.index);router.get('/export.csv',ctrl.exportCsv);router.get('/incidents.csv',ctrl.exportIncidentsCsv);router.post('/',ctrl.create);router.post('/refresh-all',ctrl.refreshAll);router.post('/bulk',ctrl.bulkAction);router.post('/:id/refresh',ctrl.refresh);router.post('/:id/healthy',ctrl.markHealthy);router.post('/:id/maintenance',ctrl.markMaintenance);module.exports=router;
