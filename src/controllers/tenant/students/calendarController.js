const {getStudent,mustHaveStudent,getStudentDisplayName,renderView}=require("./_helpers");
const {calendarVisibilityFilterForStudent}=require("../../../services/tenant/academicCalendarService");
module.exports={index:async(req,res)=>{try{const {AcademicEvent}=req.models||{};const {user,student}=await getStudent(req);if(!user)return res.redirect("/login");const blocked=mustHaveStudent(res,{tenant:req.tenant,user,student,currentPath:req.originalUrl,pageTitle:"Academic Calendar"},"students/calendar");if(blocked)return blocked;const events=await AcademicEvent.find(calendarVisibilityFilterForStudent(student)).sort({startDateKey:1,startDate:1,title:1}).limit(500).lean();return renderView(req,res,"students/calendar",{pageTitle:"Academic Calendar",user,student,studentName:getStudentDisplayName(student,user),events:events.map(e=>({...e,startKey:e.startDateKey||(e.startDate?new Date(e.startDate).toISOString().slice(0,10):""),endKey:e.endDateKey||(e.endDate?new Date(e.endDate).toISOString().slice(0,10):"")})),error:null});}catch(err){console.error("STUDENT CALENDAR ERROR:",err);return res.status(500).send("Failed to load academic calendar.");}}};
